
export interface ServiceCard {
  title: string;
  description: string;
  image: string;
}

export interface ShowcaseItem {
  before: string;
  after: string;
  title: string;
}

export interface FeatureCard {
  title: string;
  icon: string;
  description: string;
}
